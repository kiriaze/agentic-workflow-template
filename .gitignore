# Session markers (hourly, auto-created by session-start hook)
.claude/.session-*
.scripts/.session-*

# Local permission allowlists — machine-specific, not portable
.claude/settings.local.json

# macOS
.DS_Store
**/.DS_Store

# Backup files created by setup.sh when overwriting existing files
*.bak.*

# Misc
node_modules/
.env
.env.local
