# Session markers (hourly, auto-created by session-start hook)
.claude/.session-*
.scripts/.session-*

# macOS
.DS_Store
**/.DS_Store

# Backup files created by setup.sh when overwriting existing files
*.bak.*

# Misc
node_modules/
.env
.env.local
